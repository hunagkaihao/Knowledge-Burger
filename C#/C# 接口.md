# ICollection\<T>

ICollection\<T> 标准集合接口可以对其中的对象进行技术。

`方法`：

1. 可以确定集合大小（Count)
2. 确定集合中是否存在某个元素（Contains）
3. 将集合复制到一个数组（ToArray）
4. 确定集合是否为只读（IsReadOnly）
5. 对可写集合元素进行添加（Add）
6. 对可写集合元素进行删除（Remove）
7. 对可写集合元素进行清空（Clear）
8. 可以通过 foreach 语句进行遍历