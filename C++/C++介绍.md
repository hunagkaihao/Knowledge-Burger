# iostream

iostream 库包含两个基础类型 istream 和 ostream，分别表示**输入流**和**输出流**。

一个流就是一个字符序列，是从 IO 设备读出或写入 IO 设备的。

术语 ”流“（stream）想要表达的是，随着时间的推移，字符是顺序生产或消耗的。

`cin`：为 istream 类型的对象。这个对象也被称为**标准输入（standard input）**。

`cout`：为 ostream 类型的对象。这个对象也被称为**标准输出（standard output）**。

## 向流写入数据

`输出运算符（<<）`：左侧的运算对象必须是一个 ostream 对象，右侧的运算对象是要打印的值。

# 类型选择

1. 当明确知晓数值为负时，选用无符号类型
2. 使用 int 执行整数运算。在实际应用中，short 常常显得太小而 long 一般和 int 有一样的尺寸。如果数值超过了 int 的表示范围，选用 long long
3. 在算术表达式中不要使用 char 或 bool
4. 执行浮点运算选用 double



# 类模板

1. vector 

# 函数模板



# 自动对象

把只存在于块执行期间的对象称未**自动对象**



# 指针

**常量指针**：不能修改指针所指向的值，但能修改指针所指向的地址

**指针常量**：不能修改指针所指向的地址，但能修改指针所指向的值
