# 快捷键

- Alt  + H + O + I：自动调整列宽
- 选中列，将鼠标移到选中列的**边框线上**，直到光标变成 **四向箭头**（带小十字的移动图标），然后按住Shift：移动选中列到指定列
- Ctrl + 1：自定义格式

# 快速插入每行同样数据

### **使用“自定义单元格格式”（仅改变显示，不改变实际内容）**

如果你只是希望**看起来**后面有 `${05}`，但点击单元格时里面还是原来的数字，可以用这个方法。

1. 选中 B 列的所有数据。

2. 按 **Ctrl + 1** 打开“设置单元格格式”。

3. 在左侧选择 **“自定义”**。

4. 在右侧“类型”框中输入：

   ```
   @"插入的内容"
   
   // 示例
   @"${05}"
   ```

   *(@符号代表原本的文本内容)*